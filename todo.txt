[ ] pickImage:
  ios result typings
  android proper call?
  api resutl type?
  filter by video/image?
  multiple?


[ ] xxx
  getBlobHmac?
  encryption?
  readFile(path: string): Promise<BlobData>; -- add offset and length?
  writeFile(path: string, data: BlobData): Promise<void>; -- add offset and length? and truncate? incorporate appendFile?
  Blob.slice() ? test
  
